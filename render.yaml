services:
  - type: web
    name: render
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    rootDir: server
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    region: oregon  # Cambiá esto si querés otra región